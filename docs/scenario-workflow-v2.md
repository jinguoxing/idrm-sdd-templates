# 场景化工作流功能 - 详细技术方案 v2

> **版本**: v2.0  
> **创建日期**: 2026-01-03  
> **目标版本**: idrm-sdd-templates v0.4.0  
> **状态**: 待确认

---

## 一、背景分析

### 1.1 当前问题

我们的 idrm-sdd-templates 基于 Spec Kit 的标准工作流：

```
Constitution → Specify → Plan → Tasks → Implement
```

**问题**: 这个流程适合 **0→1** (全新功能)，但对于以下场景支持不足:
- 小改动 (Bug 修复)
- 功能扩展 (添加子功能)
- 大规模重构

### 1.2 竞品分析

| 框架 | 强项 | 弱项 |
|------|------|------|
| **Spec Kit** | 0→1 新功能，AI 命令集成 | 1→n 场景支持不足 |
| **OpenSpec** | 1→n 变更管理，Delta/Archive | CLI 依赖，学习曲线 |
| **IDRM (目标)** | 0→1 + 1→n 全场景 | 待实现 |

---

## 二、OpenSpec 关键设计分析

### 2.1 双层目录结构

```
openspec/
├── specs/              # 当前真相 (Source of Truth)
│   └── auth/
│       └── spec.md
└── changes/            # 变更提案 (Delta)
    └── add-2fa/
        ├── proposal.md     # 变更提案
        ├── tasks.md        # 任务清单
        ├── design.md       # 技术设计 (可选)
        └── specs/
            └── auth/
                └── spec.md # Delta (ADDED/MODIFIED/REMOVED)
```

### 2.2 Delta 格式

```markdown
## ADDED Requirements
### Requirement: 2FA Support
#### Scenario: User enables 2FA
...

## MODIFIED Requirements  
### Requirement: Login Flow
(完整的更新后文本)

## REMOVED Requirements
### Requirement: SMS Login
```

### 2.3 工作流

```
Draft Proposal → Review & Align → Implement → Archive
```

### 2.4 可借鉴点

| 能力 | 采纳建议 |
|------|----------|
| Delta 思维 (ADDED/MODIFIED/REMOVED) | ✅ 采纳 |
| changes/ 目录 | ⚠️ 可选采纳 |
| Archive 机制 | ❌ 暂不采纳 (增加复杂度) |
| CLI 命令 | ❌ 不采纳 (使用 AI 命令) |

---

## 三、GitHub Spec Kit 命令分析

### 3.1 核心命令

| 命令 | 用途 | 我们是否已有 |
|------|------|--------------|
| `/speckit.constitution` | 创建项目宪法 | ✅ 有 |
| `/speckit.specify` | 定义需求 | ✅ 有 |
| `/speckit.plan` | 技术方案 | ✅ 有 |
| `/speckit.tasks` | 任务拆分 | ✅ 有 |
| `/speckit.implement` | 执行实现 | ✅ 有 |

### 3.2 可选命令

| 命令 | 用途 | 建议 |
|------|------|------|
| `/speckit.clarify` | 需求澄清 | ⚠️ 考虑 |
| `/speckit.analyze` | 代码分析 | ✅ 有 |
| `/speckit.checklist` | 检查清单 | ✅ 有 |
| `/quizme` | 知识测验 | ❌ 不需要 |

### 3.3 发现

Spec Kit **没有场景化命令**，说明:
1. 场景化是我们可以**差异化的创新点**
2. 不需要与 Spec Kit 命名冲突

---

## 四、场景化设计方案

### 4.1 四种场景定义

| 场景 | 条件 | 工作流 | 预计时间 |
|------|------|--------|----------|
| **🆕 新功能** | specs/{feature}/ 不存在 | 完整 5 阶段 | 2-8h |
| **🔧 小改动** | 已有 spec, <50行 | 识别→更新 tasks→实现 | 30min |
| **➕ 扩展** | 添加子功能 | 扩展 spec/plan/tasks | 1-4h |
| **🔄 重构** | 破坏性变更 | 创建 v2→重新设计→迁移 | 4-16h |

### 4.2 命令设计方案对比

| 方案 | 命令 | 优点 | 缺点 |
|------|------|------|------|
| A | 4个独立命令 | 精准匹配 | 记忆负担 |
| **B** | **1个智能命令** | 简洁 | 依赖AI判断 |
| C | 2个命令 (新+改) | 平衡 | 中间方案 |

### 4.3 推荐方案 B: 智能场景命令

**命令名称**: `/speckit.start`

**工作原理**:

```
用户: /speckit.start 添加密码重置功能

AI 判断:
1. 检查 specs/user-auth/ → 存在
2. 分析: 添加子功能，不破坏现有接口
3. 判定: 场景三 (扩展)
4. 执行: 扩展工作流
```

**判断逻辑**:

```
┌─ specs/{feature}/ 存在? ─┐
│                          │
否                         是
│                          │
▼                          ▼
场景一                   分析变更规模
新功能                        │
                    ┌────┬────┴────┬────┐
                    │    │         │    │
                   <50行  添加功能  破坏性  其他
                    │    │         │    │
                    ▼    ▼         ▼    ▼
                  场景二 场景三    场景四 场景三
                  小改动  扩展      重构   扩展
```

---

## 五、文件结构设计

### 5.1 新增到 idrm-sdd-templates

```
idrm-sdd-templates/
├── .specify/
│   └── workflows/                        # 场景工作流文档 [新增]
│       ├── README.md                     # 决策树和总览
│       ├── scenario-1-new.md             # 新功能详细流程
│       ├── scenario-2-update.md          # 小改动详细流程
│       ├── scenario-3-extend.md          # 扩展详细流程
│       └── scenario-4-refactor.md        # 重构详细流程
│
├── go-zero/
│   ├── .cursor/commands/                 # [新增]
│   │   └── speckit.start.md              # 智能场景命令
│   │
│   └── .claude/commands/                 # [新增]
│       └── speckit.start.md              # 智能场景命令
```

### 5.2 安装时复制

- workflows/ → 项目的 .specify/workflows/
- .cursor/commands/ → 项目的 .cursor/commands/
- .claude/commands/ → 项目的 .claude/commands/

---

## 六、命令文件内容设计

### 6.1 /speckit.start 核心命令

```markdown
---
description: 启动 SDD 工作流 (智能场景判断)
---

## 场景化 SDD 启动

**你的需求**: $ARGUMENTS

---

### Step 1: 场景判断

我会自动分析:

1. **检查 specs/{feature}/ 目录**
   - 不存在 → 🆕 场景一: 新功能
   - 存在 → 继续分析

2. **分析变更类型**
   - 修复/优化 (<50行) → 🔧 场景二: 小改动
   - 添加子功能 → ➕ 场景三: 扩展
   - 破坏性变更 → 🔄 场景四: 重构

### Step 2: 执行对应工作流

根据判断结果，参考:
- `.specify/workflows/scenario-1-new.md`
- `.specify/workflows/scenario-2-update.md`
- `.specify/workflows/scenario-3-extend.md`
- `.specify/workflows/scenario-4-refactor.md`

### Step 3: 人工检查点

⚠️ 每个阶段完成后等待确认:
- Phase 2 (Specify) → 等待确认需求
- Phase 3 (Plan) → 等待确认方案
- Phase 4 (Tasks) → 等待确认任务

---

**参考**: `.specify/workflows/README.md`
```

---

## 七、实现计划

### Phase 1: 工作流文档 (1天)

| 任务 | 文件 | 内容 |
|------|------|------|
| T1-1 | workflows/README.md | 决策树、场景总览 |
| T1-2 | scenario-1-new.md | 5阶段完整流程 |
| T1-3 | scenario-2-update.md | 4步快速流程 |
| T1-4 | scenario-3-extend.md | 5步扩展流程 |
| T1-5 | scenario-4-refactor.md | 6步重构流程 |

### Phase 2: 命令文件 (0.5天)

| 任务 | 文件 |
|------|------|
| T2-1 | go-zero/.cursor/commands/speckit.start.md |
| T2-2 | go-zero/.claude/commands/speckit.start.md |

### Phase 3: 脚本更新 (0.5天)

| 任务 | 内容 |
|------|------|
| T3-1 | 更新 sdd-install.sh 复制 workflows/ |
| T3-2 | 更新 sdd-install.sh 复制 commands/ |
| T3-3 | 更新 sdd-upgrade.sh |
| T3-4 | 测试完整流程 |

---

## 八、决策点

请确认以下选项:

### 1. 命令设计

- [ ] 方案 A: 4个独立场景命令
- [ ] **方案 B: 1个智能命令 `/speckit.start`** (推荐)
- [ ] 方案 C: 2个命令 (`/speckit.new` + `/speckit.change`)

### 2. 是否采纳 Delta 思维

- [ ] 是: 扩展时在 spec.md 中标记 `## ADDED Requirements` 等
- [ ] 否: 保持现有的版本历史方式

### 3. 是否采纳 changes/ 目录结构

- [ ] 是: 创建 `specs/{feature}/changes/` 子目录
- [ ] **否: 直接修改现有 spec.md** (推荐)

### 4. 场景优先级

- [ ] 全部 4 个场景一起实现
- [ ] **先实现场景一和场景三** (推荐)，场景二/四后续

---

## 九、预期产出

v0.4.0 发布后:

1. **新增命令**: `/speckit.start`
2. **新增文档**: 5 个 workflow 文档
3. **更新脚本**: install/upgrade 支持
4. **用户体验**: 根据需求自动匹配场景

---

**请审核后确认决策点，我将开始实现。**
